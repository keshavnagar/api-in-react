1. Loading state: so when we use the use effect hook with api to fetch an object the object initially null so the null.property we can't access than it return the null values so to cure this we use the loading state to first when we first time run the program and the program run the if condition check is the loading is true or not if loading is true than it show the loading if loading false we see the api data
2. Error state: same thing for error state if our api data is not successfully come from the internet we need to this state that's why we set this state as true or false if api data is not coming from the internet than we create a state and put the state to show the failed message or error message on the screen with the help of this state so when our error state is set we again same to same like api data is comming and we false the loading state same we false the loading state like error state is just working when our api data state fail to load data or take data from the internet than what is the value of that state ? so the value of the state is the real error , like when we handle the promise we have two values first is our data and second is error message if an error occur so the error we store through the set error and show to the screen.
3. first i clear my all previous doubts when our projecct is running the initial value of the apidata state is null when the first time our project run with these values than there is nothing show for some time because we use the use effect our api is successfull call in second time and we see the output but in first time when our app is just rendering our apidata state has values null for that amount of time like between the first time rendering and second time rendering we need a state to handle that time so we use the loading state like if our api successfully called the loading first arrrive until our api data is not coming from the internet.
4. for your kind of information you should be read first the point number 3 first to understand all these concepts




