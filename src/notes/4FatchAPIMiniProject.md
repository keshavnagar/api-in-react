1. now we see in our new api fatching where we fatch the object that's why we set state in the form of object that's why we set the state initial value is null
2. since we write the null than if the first time our web page is render it shows error that is the properties are not define because first time it is null, in second time it will be the object and than it fatch its properties
3. so the erro that shows in 5nullErrro.png.
5. and in 6nullErrorCode.png the mistake that we do that's why we get the error that shows in above screenshot
6. so for the solutions we write the if condtiosiont that solve our propblelm

