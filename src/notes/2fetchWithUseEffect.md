# fetch api data with use effect
1. jo part tha aise hee likha tha with state usey apan use effect call back function ke andar likh denge wihtout comment the state setting
2. so when we run after adding the cahnge the network tab has only two reqeusts we also see in the screenshot []() 
3. and also working fine on display on the broweser display! [network tab](4fetchWithUseEffect.png)
4. so on that time we are facing the requests growing again and again but now we have only limited request so the reason is use effect , use effect control the fetch api with only one time when it appear with empty dpenendecy list only one time when the compoent is mount so when the state is cahnge the use effect not run again and the api call not run or fetch funcitno that contains the state not change and also set interval is has the thing to run in that one set interval to run again and again but the fetch api has not that type of funcitonality so in there we see we remove the set interval creation multiple time but the set interval internally has the feature to run again and agin but fetch has not so there is two layer in set interval but fetch has not